#!/usr/bin/env bash
# IFS


while IFS=':' read -ar line; do
    echo "The user ${lines[0]} is part of the ${lines[3]} gang, lives in ${lines[5]} and rides ${lines[6]}. ${lines[2]}'s place is protected by the passcode ${lines[1]}, more info about the user here: ${lines[4]}"
done < /etc/passwd
